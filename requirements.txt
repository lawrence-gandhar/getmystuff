bcrypt==4.0.1
passlib[bcrypt]
aiomysql
litestar[jinja]==2.21.1
multipart==1.3.1
litestar-htmx==0.5.0
uvicorn[standard]
sqlalchemy[asyncio]
python-jose[cryptography]
python-dotenv
motor
pandas
pyarrow
pydantic
cryptography
fastavro
# pandas' engine for .xlsx. Not a transitive dependency of pandas — without it
# every Excel upload fails with ModuleNotFoundError, even though XLSX is an
# advertised datasource type (see app/utils/file_utils.ALLOWED_EXTENSIONS).
openpyxl
asyncpg
aiosqlite
psycopg2-binary
anthropic
openai
pypdf
python-docx
httpx
pgvector
alembic

# --------------------------------------------------------------------------
# Deep Agents — app/services/deep_agents/
#
# deepagents requires Python >= 3.11 (it imports typing.Required), which is why
# this project runs in the Docker image rather than a local 3.10 venv. See
# documentations/DEEP_AGENTS.md.
#
# LangChain is imported ONLY inside app/services/deep_agents/. Every other AI
# path (ai_analytics_service, ai_inbuilt/ollama_client) stays on the anthropic /
# openai SDKs and raw httpx exactly as before.
# --------------------------------------------------------------------------
deepagents
langgraph
langchain-core
langchain-anthropic
langchain-openai
langchain-ollama
